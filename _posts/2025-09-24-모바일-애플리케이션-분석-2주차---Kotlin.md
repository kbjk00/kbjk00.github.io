---
title: "[모바일 애플리케이션 분석] 2주차 - Kotlin"
date: 2025-09-24 14:21:27 +0900
categories: [Projects, "모바일 분석"]
tags: [java, 팀프로젝트, "모바일 애플리케이션", kotlin]
---
이번 주차목표 모바일 애플리케이션 기초, APK 구조를 이해와

Kotlin 문법을 공부하는 것이다.

하나의 글에 내용을 담기에는 글이 너무 길어서

<br>

애플리케이션 기초와 Kotlin 을 나누어서 작성할 예정이다.

---

![](/assets/img/2025-09-24-모바일-애플리케이션-분석-2주차---Kotlin/img.webp)

Kotlin 은 IntelliJ IDEA 의 개발사인 JetBrains 에서 만든 언어이다.

※ Eclipse 에서도 Kotlin 을 사용할 수 있지만 IntelliJ에서 오류를 더 자세히 알려준다.

JVM 기반의 언어라서 Java 와 매우 유사하고 상호 운용이 가능하다.

<br>

또한 구글이 안드로이드의 공식 언어로 Kotlin 을 추가해서

안드로이드 애플리케이션을 만들 때도 많이 사용하는 중이다.

---

## Kotlin vs Java

앞에서 Kotlin 은 Java 와 유사하다고 했다.

다시 말하면, Java 와 다른 부분도 있다는 것이다.

어떤 부분이 Java 와 다른지 지금부터 소개하겠다.

<br>

---

## 진입점, 세미콜론

Hello World 를 출력하는 프로그램을 Kotlin 과 Java 로 만들어봤다.

```
fun main() {
    println("Hello World")
}
```

```
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```

차이점은 무엇일까?

먼저 진입점이 다르다.

Java 에서는 프로그램을 실행할 때 public static void main(String[] args) 이라는 메서드를 사용하였다.

<br>

그러나 Kotlin 에서는 fun main() 이라는 함수를 사용하였다.

함수와 메서드의 차이는 클래스의 유무 차이이다.

함수는 클래스 내부에 없어서 독립적이고

<br>

메서드는 클래스 내부에 있어서 종속적이다.

또한 Kotlin 에서 println() 함수를 보면 마지막에 세미콜론(;) 이 존재하지 않는다.

이 또한 Java 와 다른 부분이다.

<br>

Java 는 세미콜론이 필수적으로 사용된다.

---

## 변수 선언 방식

Kotlin 은 Java 와 다르게 var, val 로 변수 선언을 시작하고 변수명 뒤에 타입을 지정한다.

var 는 값을 변경할 수 있는 변수, val 은 값을 변경할 수 없는 변수를 선언할 때 사용된다.

```
fun main() {
	var num1: Int = 222
    num1 = 200
    
    val num2: Int = 333
    num2 = 300
    
    println(num1)
    println(num2)
}
```

이런 프로그램을 실행하면 num1, num2 는 어떻게 출력될까?

```
C:\Users\사용자이름\IdeaProjects\KotlinTest\src\Main.kt:6:5
Kotlin: 'val' cannot be reassigned.
```

val 을 사용한 num2 는 값을 바꿀 수 없기 때문에 오류가 발생한다.

num2 = 300을 주석 처리한 후 num1 의 값이 변경되는지 확인해보겠다.

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=57771" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
200
333
```

num1 은 값을 변경할 수 있는 것을 확인할 수 있다.

---

## Nullable

Java 는 모든 변수를 기본적으로 Nullable 로 정의한다.

그리고 null-check 구문을 만들어 NPE(Null Pointer Exception) 을 피하려 한다.

※ NPE : null 로 인해 발생하는 Runtime Exception

<br>

Kotlin 에서는 null 을 허용하지 않는 Non-Nullable 타입이 존재한다.

그래서 null 을 사용하고 싶다면 변수 타입에 ? 를 붙여 Nullable 타입으로 지정할 수 있다.

이 덕분에 NPE 를 줄일 수 있는 장점이 있다.

<br>

```
fun main() {
    val num1: Int? = null
    println(num1)

    val num2: Int = null
    println(num2)
}
```

이 프로그램을 실행하면 어떻게 될까?

```
C:\Users\사용자이름\IdeaProjects\KotlinTest\src\Main.kt:5:21
Kotlin: Null cannot be a value of a non-null type 'Int'.
```

num2 가 Non-Nullable 타입으로 지정되있지만 null 을 대입하여 오류가 발생한다.

num1 만 출력하도록 주석 처리하고 null 이 출력되는지 살펴보겠다.

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=60137" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
null
```

num1 이 Nullable 임을 확인할 수 있다.

## ※ !! 문법

변수 뒤에 !! 을 사용하여 변수가 null 이 아님을 지정하는데 사용한다.

null 에 대한 처리를 하는 것이 아니라

강제로 null 이 아님을 명시할 수 있는 것이라서 권장하지는 않는다.

<br>

---

## 엘비스 연산자

엘비스 연산자는 null 을 활용한 연산자이다.

?: 로 엘비스 연산자를 사용할 수 있다.

엘비스 연산자는 어떻게 활용할 수 있을까?

<br>

예를 들어 특정 변수가 null 인 경우 특정한 값을 반환시킬 수 있다.

```
fun main() {
    val num: Int? = null

    print(num ?: "num is Nullable")
}
```

이 프로그램의 경우 실행하면 num 에 null 이 있어서 문자열이 출력된다.

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=65100" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
num is Nullable
```

---

## when() 조건문

Kotlin 에는 Java 의 switch 조건문과 유사한 when 조건문이 있다.

switch 처럼 case 마다 조건을 설정할 수 있다.

```
fun main() {
	val num: Int = 3
    
    when (num) {
    	1 -> print("Your number is 1.")
        2 -> print("Your number is 2.")
        3 -> print("Your number is 3.")
        else -> print("I can't know your number.")
        }
}
```

이 프로그램을 실행하면 Your number is 3. 이 출력된다.

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=58344" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
Your number is 3.
```

또한 when 을 사용하여 변수에 바로 값을 지정할 수 있다.

```
fun main() {
    val num: Int = 3

    val output = when (num) {
        1 -> 1
        2 -> 2
        3 -> 3
        else -> 0
    }
    
    print(output)
}
```

실행하면 output 에 3으로 값이 지정된 것을 알 수 있다.

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=61366" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
3
```

---

## 함수 선언

```
fun main() {
    val Msg1 = fun1(1)
    val Msg2 = fun2(2)

    println(Msg1)
    println(Msg2)
}

fun fun1(a : Int): String {
    return "I am Msg1"
}

fun fun2(a : Int): String = "I am Msg2"
```

이 프로그램에서 2개의 함수를 선언하였다.

파라미터 타입을 지정하고, 리턴 타입 또한 같은 형식으로 지정하면 된다.

fun2 처럼 함수가 매우 짧은 경우 중괄호를 사용하지 않고 = 를 사용하여 이어적어도 된다.

<br>

또한 Kotlin 은 메서드 오버로딩을 지원한다.

함수명이 같아도 파라미터가 다르다면

다른 동작을 할 수 있도록 구현할 수 있다.

<br>

```
fun main() {
    val Msg = fun1(1)
    val Msg2 = fun1(2, 3)

    println(Msg)
    println(Msg2)
}

fun fun1(a : Int): String {
    return "return"
}

fun fun1(a : Int, b : Int): String {
    return  "overloaded function's return"
}
```

이 프로그램의 경우 fun1 로 함수의 이름은 같지만 파라미터가 다르다.

a 만 사용하는 경우 return 을 반환하고

a, b 를 사용하는 경우 overloaded function's return 을 반환한다.

<br>

```
"C:\Program Files\Java\jdk-24\bin\java.exe" "-javaagent:C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2025.2.1\lib\idea_rt.jar=60211" -Dfile.encoding=UTF-8 -Dsun.stdout.encoding=UTF-8 -Dsun.stderr.encoding=UTF-8 -classpath C:\Users\김범준\IdeaProjects\KotlinTest\out\production\KotlinTest;C:\Users\김범준\.m2\repository\org\jetbrains\kotlin\kotlin-stdlib\2.2.0\kotlin-stdlib-2.2.0.jar;C:\Users\김범준\.m2\repository\org\jetbrains\annotations\13.0\annotations-13.0.jar MainKt
return
overloaded function's return
```

오버로딩 때문에 함수 이름이 같아도 파라미터가 달라

오류 없이 프로그램이 제대로 실행되는 것을 볼 수 있다.

---

## 후기

Kotlin 이 안드로이드 공식 언어로 추가되었지만

단순히 모바일 애플리케이션 개발에만 사용될 언어는 아니라고 생각한다.

Java 와 상호 운용이 가능해서 Java 에서 Kotlin 으로 넘어가는 사람들이 많다.

<br>

만약 기존에 Java 를 사용하고 있다면,

Kotlin 도 쉽게 배울 수 있을테니 같이 사용해 보는 것도 좋을 것 같다.
